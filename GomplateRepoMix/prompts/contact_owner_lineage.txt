## ENTITY CARD LOAD — STATE BEFORE RENDERING
## Usage: claude -p < contact_owner_lineage.txt > owner_lineage.txt

Entity card: context/cards/contact.yaml
Load this card first. All decisions in this prompt are governed by the card.

Medallion position:
  Bronze  → staging.stg_contact
  Silver  → staging.stg_contact_normalised
  Gold    → hubspot.contacts
  dbt     → NOT in dbt boundary (direct Bronze→Silver→Gold)

Primary key:  Pers_PersonId  →  icalps_contact_id (bigint)
StackSync resolution column:  stacksync_record_id_nd85zc
FK dependencies:
  Company (REJECT) → stg_contact_normalised.pers_companyid
                   → hubspot.companies.icalps_company_id
Import order: 2 (Company must be upserted FIRST)

Cardinality:  Contact N:1 Company, N:M Opportunity, 1:N Communication

Owner lineage:
  Fields available: owner_firstname, owner_lastname (NO owner_email)
  Resolution: name match against HubSpot owner list
  Severity: WARN (not STOP unless owner_resolution_blocking=true)

Dedup scorer:  Levenshtein (email is highest signal at weight 0.65)
Dedup thresholds:  review ≥ 0.60, block ≥ 0.80

Pipeline status:  live_push_ready=true
Runner arg:  --entity contact

---

## CONTEXT: You are an IC'ALPS pipeline engineer

You are working on the Silver normalization and Gold upsert layers of a
StackSync-mirrored PostgreSQL CRM database. The entity card above is the
canonical state of this entity in the medallion.

Dedup key fields and weights (from card):
  icalps_email (email strategy)    → weight 0.65  ← highest signal
  full name (levenshtein)          → weight 0.20
  pers_companyid (exact)           → weight 0.10
  phone digits (digits)            → weight 0.10

Intra-candidate dedup checks:
  duplicate_primary_key on pers_personid
  duplicate_identity_signature: "email|pers_firstname|pers_lastname|pers_companyid"

Company association timing:
  After STACKSYNC_SYNC — child companies must already exist in hubspot.companies
  Filter: hubspot.companies.domain ~ '^[0-9]+\.' (children only)
  NOT EXISTS guard prevents re-updating already-associated contacts

Silver validation STOP checks:
  no_duplicate_icalps_contact_id
  no_null_pers_personid
  fk_pers_companyid_in_hubspot_companies (REJECT)

---

## QUESTIONS — produce executable SQL for each
## Output will be piped to owner_lineage.txt

1. Write the Gold upsert SQL for hubspot.contacts.
   - Conflict key: icalps_contact_id
   - Filter: _load_status IN ('NEW', 'MODIFIED')
   - Resolve pers_companyid to hubspot.companies.id via subquery for associatedcompanyid
   - Apply UTF-8 cleaning on firstname, lastname
   - Exclude createdate and stacksync_record_id_nd85zc from UPDATE SET
   - Source: staging.stg_contact_normalised

2. Write the Silver dedup SQL for contacts using prefer_record_with_most_metadata.
   Count non-null columns per pers_personid group, keep max, tie-break ASC.
   Output: canonical row per pers_personid.

3. Write the Silver validation SQL for all STOP checks.
   Return: check_name, passed (bool), severity, row_count_failing.
   Include the FK REJECT check that verifies pers_companyid is in hubspot.companies.

4. Write the owner name resolution diagnostic SQL.
   Join stg_contact_normalised.owner_firstname + owner_lastname against a
   hypothetical owner reference table. Return:
   owner_name, found_in_reference (bool), contact_count.
   Include a comment noting that owner_email does NOT exist in stg_contact_normalised.

5. Write the SQL that updates hubspot.contacts.associatedcompanyid after STACKSYNC_SYNC.
   Join via pers_companyid → hubspot.companies.icalps_company_id → id.
   Apply child-only filter (domain ~ '^[0-9]+\.').
   Use NOT EXISTS to prevent re-updating contacts already correctly associated.

6. Write the Levenshtein duplicate detection SQL for contacts.
   Compare full name (firstname + lastname) against benchmark export using
   PostgreSQL's levenshtein() function or pg_trgm similarity().
   Flag pairs where similarity >= 0.60 AND same company (pers_companyid match).
   These are review candidates, not auto-blocks.

7. State the exact runner CLI invocation for a Contact run with owner resolution
   in warn (non-blocking) mode:
     python -m pipeline.runner --entity contact ...

## ENTITY CARD LOAD — STATE BEFORE RENDERING
## Usage: claude -p < communication_association.txt > output.txt

Entity card: context/cards/communication.yaml
Load this card first. All decisions in this prompt are governed by the card.

Medallion position:
  Raw     → staging.raw_stg_communication       (77,100 rows)
  Bronze  → staging.stg_communication           (29,150 rows)
  Silver  → staging.stg_communication_normalised (6,127 rows — with BOTH anchors)
  dbt:      int_communication_classified
            int_communication_reconciled
  Gold:     hubspot.calls / hubspot.notes / hubspot.tasks
  Deferred: hubspot.meetings (NOT in active association bridge)

Primary key:  Comm_CommunicationId  →  icalps_communication_id (bigint)
Idempotency key:  unique_id = 'icalps_' || icalps_communication_id::text
StackSync:  Resolution uses unique_id, NOT a single stacksync_record_id column

FK dependencies (all WARN):
  Company → legacy_company_id → hubspot.companies.icalps_company_id
  Contact → legacy_contact_id → hubspot.contacts.icalps_contact_id
  Deal    → legacy_deal_id    → hubspot.deals.icalps_deal_id
Import order: 4 (all parent entities must be upserted FIRST)

Anchor completeness rule:
  Zero rows in stg_communication_normalised lack BOTH company AND person anchors.
  This is a STOP check. It was verified in live staging snapshot 2026-04-04.

Hierarchy unflattening (QA/probe only — not production write path):
  Algorithm: reverse-depth-first
  Levels: Level_000=Company_Name, Level_001=Person name, Level_002=Subject/ID
  NodeKey: "|".join(path_parts)

Two-pass association bridge:
  Pass A: fct.associated_*_id → target.stacksync_record_id_*  (UUID path)
  Pass B: fct.legacy_*_id     → target.icalps_*_id            (fallback)
  Idempotency: NOT EXISTS on association table
  Set operation: UNION (not UNION ALL)
  Prerequisite for Pass A: STACKSYNC_SYNC must have completed

Association probe priority (from card, by live UUID readiness):
  1. Calls → company   (99.7% UUID ready) ← PROBE FIRST
  2. Calls → contact   (79.8%)
  3. Notes → contact   (71.0%)
  4. Notes → company   (12.6%)
  5. Notes → deal      (2.1%)
  6. Tasks → company   (17.2%)
  7. Tasks → contact   (16.6%)
  DEFERRED: Meetings (0.8% UUID ready — do NOT probe)

Dedup: unique_id-based only. No scorer needed for key dedup.
MCP fallback: optional for note content similarity comparisons.

Pipeline status:
  engagement upsert: live_push_ready=true
  association bridge: opt-in via --enable-post-gold
Runner arg:  --entity communication

---

## CONTEXT: You are an IC'ALPS pipeline engineer

You are working on the dbt communication pipeline, Silver normalization,
and mirrored association bridge layers. The entity card is the canonical state.

int_communication_classified maps Comm_Action to HubSpot activity class.
int_communication_reconciled attaches resolved stacksync_record_id_* values.
The fct_communication_* marts expose both UUID (associated_*_id) and
legacy (legacy_*_id) anchors for the two-pass bridge.

Duplicate prevention:
  unique_id (icalps_ prefix) prevents duplicate engagement inserts
  NOT EXISTS prevents duplicate association rows in both passes
  UNION prevents the same pair appearing in both Pass A and Pass B results

Meetings deferral rule:
  Do NOT add Meetings to any SQL, association pattern, or schema_context.yaml
  entry until: (1) UUID readiness > 50%, (2) type IDs confirmed from portal.

---

## QUESTIONS — produce executable SQL and dbt models for each
## Output will be piped to output.txt

1. Write the int_communication_classified dbt model SQL.
   Map Comm_Action → hubspot_activity_class: calls/notes/tasks/meetings.
   Output columns: icalps_communication_id, comm_action_raw, hubspot_activity_class,
   plus all original staging columns.
   Meetings classification: include in the model but mark with a comment
   noting that Meetings are excluded from the active association bridge.

2. Write the int_communication_reconciled dbt model SQL.
   Join stg_communication_normalised (via legacy IDs) to:
     hubspot.companies  → associated_company_id (stacksync_record_id_9vpp8v)
     hubspot.contacts   → associated_contact_id (stacksync_record_id_nd85zc)
     hubspot.deals      → associated_deal_id    (stacksync_record_id_87b7vd)
   Output both associated_*_id (UUID) AND legacy_*_id (int) for each entity.
   NULL on UUID column = record not yet synced (→ Pass B fallback).

3. Write the full two-pass association SQL for Calls → Company (type_id=182).
   Use staging.fct_communication_calls as the bridge table.
   Pass A: fct.associated_company_id → hubspot.companies.stacksync_record_id_9vpp8v
   Pass B: fct.legacy_company_id     → hubspot.companies.icalps_company_id
   Both passes: NOT EXISTS on hubspot.associations_calls_company
   UNION between passes. Include unique_id LIKE 'icalps_%' filter.

4. Write the full two-pass association SQL for Notes → Contact (type_id=202).
   Same structure but for staging.fct_communication_notes and hubspot.contacts.

5. Write the Silver validation SQL for stg_communication_normalised:
   (a) STOP: zero rows lack both company AND person anchors
   (b) STOP: no duplicate icalps_communication_id
   (c) STOP: all unique_id values follow 'icalps_' prefix
   (d) WARN: fk_legacy_company_id in hubspot.companies
   (e) WARN: fk_legacy_contact_id in hubspot.contacts
   Return: check_name, passed, severity, row_count_failing.

6. Write the read-only probe SQL that determines association readiness
   before any insert. For each comm_type (Calls/Notes/Tasks):
   Return: comm_type, total_rows, uuid_ready_count, uuid_ready_pct,
           legacy_only_count, unreconciled_count.
   Include a comment: "Probe this SQL before any ASSOC_VALIDATE run."

7. State the full runner CLI for:
   (a) Engagement upsert only (no associations):
       python -m pipeline.runner --entity communication ...
   (b) Association-only run (parent entities already synced):
       python -m pipeline.runner --entity communication --assoc-only ...
   (c) Full post-gold run with associations:
       python -m pipeline.runner --entity communication --enable-post-gold ...

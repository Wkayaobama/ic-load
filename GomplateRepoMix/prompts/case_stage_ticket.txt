## ENTITY CARD LOAD — STATE BEFORE RENDERING
## Usage: claude -p < case_stage_ticket.txt > case_stage.txt

Entity card: context/cards/case.yaml
Load this card first. All decisions in this prompt are governed by the card.

Medallion position:
  Bronze  → staging.stg_cases              (raw, legacy preservation)
  Silver  → staging.stg_case               (ticket-shaped, 58 rows)
  Gold    → hubspot.tickets                (NOT YET LIVE)
  dbt     → NOT in dbt boundary

Primary key:  Case_CaseId  →  icalps_case_id (bigint — TO BE CONFIRMED)
StackSync resolution column:  TBD — must be confirmed from HubSpot portal
FK dependencies:
  Company (WARN) → stg_case.icalps_company_id → hubspot.companies.icalps_company_id
  Contact (WARN) → stg_case.icalps_contact_id → hubspot.contacts.icalps_contact_id
Import order: 5 (after Company, Contact, Opportunity, Communication)

Cardinality:  Case N:1 Company, N:1 Contact

Entity fields (22 columns):
  Core: Case_CaseId, Case_PrimaryCompanyId, Case_PrimaryPersonId, Case_AssignedUserId,
        Case_Status, Case_Stage, Case_Priority, Case_Description (long-form text),
        Case_Opened, Case_Closed
  Denormalized: Company_Name, Company_WebSite, Person_FirstName, Person_LastName,
                Person_EmailAddress (from vEmailCompanyAndPerson view)

Staging assessment (CURRENT STATE):
  58 source rows, 22 shaped columns
  43/58 rows match live staging.stg_case exactly (74.1% match rate)
  Residual mismatches in: createdate, icalps_case_stage, some contact/text fields
  Must reach >95% before live push candidate

Stage mapping:  NOT IMPLEMENTED
  Stage values from IC'ALPS: Open, Closed, In Progress, Pending, Escalated
  HubSpot ticket stage IDs: MUST be confirmed from portal before any mapping
  Pattern: same non-negotiable constraint as deal_stage_mapper
  Target module: context/algorithms/case_stage_mapper.py (MISSING)

Association type IDs:  ALL TBD — must confirm from HubSpot portal
  Ticket → Company, Ticket → Contact, Ticket → Deal

Text normalization for Case_Description:
  Preserve meaningful line breaks
  Strip only unsafe control characters
  Apply UTF-8/mojibake cleaning
  Do NOT strip all whitespace

Dedup scorer:  Levenshtein (subject at weight 0.45)
Dedup thresholds:  review ≥ 0.65, block ≥ 0.82
Reference ID column: IcAlps_TicketID (in benchmark CSV)

Pipeline status:  live_push_ready=FALSE
Blockers (from card):
  1. case_stage_mapper module missing
  2. Association type IDs not confirmed
  3. Staging match rate only 74.1% (must reach >95%)
  4. stacksync_record_id_* column name not confirmed

---

## CONTEXT: You are an IC'ALPS pipeline engineer

You are working on the Case/Ticket staging assessment, Bronze shaping,
and pre-production validation layers. The entity card above documents the
current state. This entity is NOT production-active. Your output is
preparatory work toward meeting the promotion checklist in the card.

All SQL you produce is safe-side only (no hubspot.* writes).
The output (case_stage.txt) should be usable as a reference for the
operator who will eventually promote Case/Ticket to live-push-ready.

Dedup key fields and weights (from card):
  subject (levenshtein)           → weight 0.45
  icalps_company_id (exact)       → weight 0.20
  icalps_contact_email (email)    → weight 0.15
  icalps_case_stage (exact)       → weight 0.10
  icalps_case_status (exact)      → weight 0.05
  hs_ticket_priority (exact)      → weight 0.05

---

## QUESTIONS — produce executable SQL for each
## Output will be piped to case_stage.txt

1. Write the Bronze → staging shaping SQL for staging.stg_case from staging.stg_cases.
   Include all 22 expected columns.
   Apply UTF-8 cleaning on Case_Description: preserve line breaks, strip control chars.
   Normalize createdate to ISO-8601 (YYYY-MM-DD format).
   Add a comment on each column mapping noting whether it is a direct field,
   a JOIN-derived field, or a computed/normalized field.

2. Write the Silver dedup SQL for stg_case using prefer_record_with_most_metadata.
   Count non-null columns per Case_CaseId group, keep max, tie-break ASC.

3. Write the Silver validation SQL for all STOP and WARN checks from the card.
   For icalps_case_stage validation: check against a known_stage_values list
   (Open, Closed, In Progress, Pending, Escalated).
   Return: check_name, passed, severity, row_count_failing.
   Include a comment on the stage_mapping check: "BLOCKED — case_stage_mapper not yet implemented."

4. Write a placeholder stage mapping SQL CASE expression for icalps_case_stage → hubspot_stage_id.
   Use NULL::int for all values.
   Add a comment block explaining:
   - The exact HubSpot API query needed to retrieve ticket stage IDs
   - The portal ID (9201667) to use
   - The format the response will be in
   - Where to paste the confirmed IDs once retrieved

5. Write the read-only Bronze assessment probe SQL.
   Compare staging.stg_cases-derived shape against staging.stg_case row by row.
   Return per-column mismatch count and mismatch percentage.
   Flag columns with >5% mismatch rate as "high residual — requires investigation".

6. Write the promotion checklist validation SQL.
   A series of checks that must ALL pass before the entity is promoted:
   (a) staging match rate >= 95%
   (b) no duplicate icalps_case_id
   (c) stacksync_record_id column exists in hubspot.tickets (information_schema check)
   (d) at least one known ticket stage mapping exists in a case_stage_mappings reference table
   (e) at least one ticket association type ID is confirmed (from a reference table)
   Return: check_name, passed, current_value, required_value.

7. Write the Levenshtein duplicate detection SQL for Case/Ticket.
   Compare subject against benchmark "Ticket name" column.
   Flag pairs where similarity >= 0.65 AND same icalps_company_id.
   These are review candidates, not auto-blocks.

8. State what must happen before Case/Ticket becomes live-push-ready:
   The exact sequence of steps, which CLI commands to run for verification,
   and which runner.py changes are needed to add 'case' as a supported entity.

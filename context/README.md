# Context

This directory will hold the fixed and per-run contract for `ic-load`.

Planned contents:
- schema context loaders
- run context loaders
- DB/environment contract
- mapping and threshold helpers

This layer should stay small and deterministic.

# SQL

This directory will hold the schema-governed SQL layer.

Planned structure:
- `templates/` for Gomplate sources
- `rendered/` for generated SQL
- optional thin execution helpers if needed

This is where idempotent upsert and association patterns should live.

interface:
  display_name: "Pipeline Salvation"
  short_description: "Salvage messy CRM/data pipelines safely"
  default_prompt: "Use $pipeline-salvation to recover the functional core of a messy CRM or sync-heavy pipeline into a clean, staged, reproducible repo before any live production write."
